spring:
  config:
    activate:
      on-profile: local

server:
  port: 8090
  forward-headers-strategy: framework
---
spring:
  config:
    activate:
      on-profile: dev

server:
  port: 8080
  forward-headers-strategy: framework
